/** REGRAS PARA NOME DE VARIÁVEIS */

/** 
 * 1 - Case-sensitive : Javascript Diferencia letras maiúsculas de minusculas,ou seja,
 *  "nome", "NOME" ou "Nome" são variáveis diferentes.
 * 
 * 2 - Começar com letra, '$' ou '_', NÃO podendo começar com números ou outros caracteres.
 * 
 * 3 - Não usar palavras reservadas.
 * * 
*/

/**
 * CONVENÇÕES DE NOMECLATURA
 * 
 * 1 - Use camelCase para nomes de variável composto: 'nomeUsuario'.
 * 
 * 2 - Use letras maiúsculas para nomes de variaveis CONSTANTES: 'TAXA_JUROS'.
 * 
 */

/** Exemplo de variáveis válidas: */
var nome;
var $precoProduto;
let _qtdEstoque;
let usuario1;
var NomeCompleto;
const TAXA_BASE = 0.1;
